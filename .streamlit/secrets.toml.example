# Copia este archivo como secrets.toml (NO subas secrets.toml a Git).
# En Streamlit Cloud: Settings → Secrets → pega el contenido.
#
# Solo úsalo si TÚ eres el único usuario de la app desplegada.
# Para equipos, es más seguro que cada persona ingrese su propia key en la web.

GEMINI_API_KEY = "tu_api_key_aqui"
